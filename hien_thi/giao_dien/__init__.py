"""
hien_thi.giao_dien — THEME / phong cách dùng chung.
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Bảng màu, font, hằng số style, QSS dùng chung cho toàn app:

    from hien_thi.giao_dien.theme import Theme

Hiện rỗng (scaffold). Đích di trú Phase sau cho lớp `Theme` đang TRÙNG ở
`man_hinh/bieu_do_nen.py` và `man_hinh/toi_uu.py`, cùng khối QSS cửa sổ trong
`hien_thi/app/ung_dung.py`.
"""
